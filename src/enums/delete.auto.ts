// auto-generated

import { EnumHandler } from "./interface";

const data = {
  "name": "delete",
  "documentation": "Command enum: delete",
  "values": [
    {
      "value": "pop"
    },
    {
      "value": "remove"
    }
  ]
};

export const deleteEnum = new EnumHandler(data);
