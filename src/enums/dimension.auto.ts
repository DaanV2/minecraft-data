// auto-generated

import { EnumHandler, Enum } from "./interface";

const data: Enum = {
  "name": "dimension",
  "documentation": "Command enum: Dimension",
  "values": [
    {
      "value": "overworld"
    },
    {
      "value": "nether"
    },
    {
      "value": "the_end"
    }
  ]
};

export const dimensionEnum = new EnumHandler(data);
